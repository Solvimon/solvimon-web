# This workflow has been merged into create-release-tag.yml.
# Kept as an empty file to avoid breaking any references.
name: Release (deprecated)

on:
    workflow_dispatch:

jobs:
    noop:
        runs-on: ubuntu-latest
        steps:
            - run: echo "This workflow is no longer active. See create-release-tag.yml."
