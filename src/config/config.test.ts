import type { Config } from './types';

export const testConfig: Config = {
    apiUrls: {
        identity: 'https://identity.solvimon.com/v1',
        config: 'https://test.api.solvimon.com/v1',
        transaction: 'https://test.api.solvimon.com/v1',
        event: 'https://test.api.solvimon.com/v1',
    },
};
