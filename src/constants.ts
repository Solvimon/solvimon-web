export const COMPONENT_TAG_PREFIX = 'solvimon-';

// Expands every expandable property on a resource (serialized as `expand[]=ALL`),
// replacing the exhaustive per-property expand lists. See DD-3042.
export const EXPAND_ALL = ['ALL'];
