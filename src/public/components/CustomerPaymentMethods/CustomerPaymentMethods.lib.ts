/**
 * How many payment methods the block shows when the consumer does not say. Shared with the customer
 * overview screen, which renders the same block and has to cap it to the same number.
 */
export const DEFAULT_MAX_ITEMS = 3;
